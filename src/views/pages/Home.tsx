import { DemoLink } from "helpers/components";

const Home = () => {
  return (
    <div>
      <p className="font-bold text-blue-500">Deployed in Netlify here:</p>
      <DemoLink />
    </div>
  );
};

export default Home;
